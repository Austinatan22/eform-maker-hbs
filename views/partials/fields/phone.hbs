<div class="mb-3">
  <label class="form-label">
    {{label}}{{#if required}} <span class="text-danger">*</span>{{/if}}
  </label>
  <!-- added js-intl-tel class -->
  <input class="form-control {{customClass}} js-intl-tel"
         type="tel"
         name="{{name}}"
         placeholder="{{placeholder}}"
         {{#if required}}required{{/if}}>
</div>
